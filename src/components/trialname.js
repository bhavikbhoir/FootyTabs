import React, { Component } from 'react';
// import Landingpage from './landingpage';

const Trialname = (props) => {
    return (
            <p className="team-position">
                {props.name}
            </p>
    );
};

export default Trialname;